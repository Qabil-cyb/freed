// Smoke test scaffold for the spider_vpn app.
// Replace with real widget tests as the UI stabilizes.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('package resolves', () {
    expect(true, isTrue);
  });
}
